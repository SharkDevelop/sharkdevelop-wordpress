# Copy this file to scripts/deploy-dev.local.env when local values differ.
# scripts/deploy-dev.local.env is ignored by Git.

# Local WordPress path. In this project Local app/public points to the repo.
LOCAL_PUBLIC="/Users/olgastennikova/Local Sites/sharkdevelop/app/public"

# If wp-cli is not available as "wp", set an explicit command, for example:
# WP_CLI_COMMAND="php /Users/you/bin/wp-cli.phar"

# If PHP is not available as "php", the script tries to find Local's PHP.
# You can pin it explicitly:
# PHP_BIN="/Users/you/Library/Application Support/Local/lightning-services/php-8.2.29+0/bin/darwin-arm64/bin/php"

LOCAL_URL_HTTP="http://sharkdevelop.local"
LOCAL_URL_HTTPS="https://sharkdevelop.local"

SSH_HOST="213.130.145.225"
SSH_PORT="65002"
SSH_USER="u781562985"

DEV_URL="https://dev.sharkdevelop.com"
DEV_PATH="/home/u781562985/domains/sharkdevelop.com/public_html/dev"
DEV_DB="u781562985_devsite"

# Keep this as 0 until the first successful media sync is verified.
# Set to 1 only if the dev uploads folder should exactly mirror local uploads.
DELETE_REMOTE_UPLOADS=0
